name: Workflow Run
on: 
  workflow_run:
    workflows: [FirstWorkflow, events, Checkout, Simple action]
    types: [completed]
jobs:
  echo-string:
    runs-on: ubuntu-latest
    steps:
      - run: echo "I was triggered by completion of the 'events' workflow."