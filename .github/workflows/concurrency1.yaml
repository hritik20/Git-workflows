name: Concurrency 1
on:
    workflow_dispatch:
        inputs:
          environment:
            description: "Enviroment"
            type: environment
            required: true

#concurrency: concurrency1
concurrency:
  group: concurrency1
  cancel-in-progress: true
jobs:
    sleep:
        runs-on: ubuntu-latest
        steps:
          - run: sleep 30