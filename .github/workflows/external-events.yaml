name: External Events
on:
    repository_dispatch:
        types: [build]

jobs:
    echo-string:
        runs-on: ubuntu-latest
        steps:
           - run: echo "This workflow runs on repository_dispatch event!"